# Core LLM / structured output
# anthropic>=0.120.0
langchain-groq
pydantic>=2.6

# Stage 4 (graph orchestration) - included now so the env is ready when we get there
langgraph>=0.2

# Config / env management
python-dotenv>=1.0

# Data handling (Stage 2/3 will consume products.csv / inventory_pricing.csv / customers.json)
pandas>=2.0

# Typing backport used by schemas/state.py (Annotated/NotRequired on py<3.11)
typing_extensions>=4.9

# Dev / testing
pytest>=8.0
